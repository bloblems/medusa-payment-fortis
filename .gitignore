node_modules
*.log
.DS_Store

# dist/ is committed so GitHub-installed consumers don't need to run a
# prepare-time build. Pre-publishing to npm via `pnpm prepublishOnly` will
# clean + rebuild it before any release.

.env
.env.local
.env.*.local
.env.development
.env.production

.vscode
.idea
*.tsbuildinfo
